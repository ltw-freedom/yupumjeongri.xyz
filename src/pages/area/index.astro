---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import CTA from '../../components/CTA.astro';
import { regions } from '../../data/areas';
import { districtCities } from '../../data/districts';
import { site } from '../../data/site';
import { localBusiness } from '../../data/schema';

const citiesByRegion = (slug: string) => districtCities.filter((city) => city.region === slug);
---

<BaseLayout
  title={`서비스 지역 | 서울 · 경기 유품정리 - ${site.name}`}
  description="서울과 경기 주요 지역의 유품정리 서비스 안내입니다. 지역별 현장 특성과 비용에 영향을 주는 요소를 정리했습니다."
  jsonLd={[localBusiness()]}
>
  <Breadcrumb items={[{ label: '지역' }]} />

  <section class="section">
    <div class="container">
      <h1>서비스 지역</h1>
      <p class="lead">
        서울 25개 구, 경기 31개 시·군 전 지역에서 진행합니다. 법정동 단위까지 지역별 안내 페이지를
        제공합니다.
      </p>

      {
        regions.map((region) => (
          <>
            <h2>
              <a href={`/area/${region.slug}/`}>{region.name}</a>
            </h2>
            <p>{region.summary}</p>
            <ul class="link-list">
              {citiesByRegion(region.slug).map((city) => (
                <li>
                  <a href={`/area/${region.slug}/${city.slug}/`}>{city.name} 유품정리</a>
                </li>
              ))}
            </ul>
          </>
        ))
      }
    </div>
  </section>

  <CTA />
</BaseLayout>
